/// Playback engine wrapper — full implementation in Phase 2.
///
/// Wraps just_audio + audio_service + audio_session.
/// Provides: play, pause, seek, chapter navigation, sleep timer,
/// position tracking, background audio, lock screen controls.
class PlaybackService {
  // Phase 2 will implement this with just_audio + audio_service.
  // For Phase 1, this is a placeholder to enable the provider layer
  // and UI to compile.

  void dispose() {}
}
