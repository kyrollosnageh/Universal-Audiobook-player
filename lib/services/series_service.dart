/// Series tracking, auto-advance, and reading order — Phase 5.
///
/// Detects series from server metadata, tracks progress,
/// provides auto-advance between books.
class SeriesService {
  // Phase 5 will implement series detection and tracking.

  void dispose() {}
}
